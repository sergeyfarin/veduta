engine-strict=true
# Exact versions: `pnpm add` and `pnpm update` write "1.2.3", never "^1.2.3". Dependency
# updates should be a reviewable commit, not something a fresh install decides on its own.
save-exact=true
save-prefix=
