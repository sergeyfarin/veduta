# Negative fixture: the semantic layer must report this.
expect: "is not declared in the integrations list"
config:
  sections:
    - title: Media
      cards:
        - id: rogue
          integration: nowhere
          operation: x
